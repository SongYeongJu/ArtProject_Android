<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.artproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ChattingActivity"></activity>
        <activity android:name=".Activities.BuyActivity" />
        <activity android:name=".Activities.CheckTasteActivity" />
        <activity android:name=".Activities.ItemInfoActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.JoinActivity" />
        <activity android:name=".Activities.Login1Activity" />
        <activity android:name=".Activities.SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>>
        <activity android:name=".Activities.MainActivity">
        </activity>
    </application>

</manifest>